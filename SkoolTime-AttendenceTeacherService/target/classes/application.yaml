#Attendence Teacher Service
server:
  port: 9004
  
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://localhost:3306/skooltime
    username: root
    password: viswanadh
  application:
    name: Attendence-Teacher-Service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:9761/eureka 
